#include <stdio.h>

//두 변수의 갑을 변환
void swap(int *x, int *y){
  int temp;
  temp=*x; //포인터 가리키고 있는 특정한 위치에 있는 값을 가져옴
  *x=*y;
  *y=temp;
}

int main(){
  int x=1;
  int y=2;
  swqp(&x, %y);
  print("x=%d\ny=%d", x, y);
  return 0;
}

/*
'*'은 변수가 가리키는 값이고
'&'은 변수가 저장되어 있는 공간의 주소
*/
